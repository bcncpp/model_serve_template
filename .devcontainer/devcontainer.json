{
  "name": "PyTorch Autoencoder Dev Container",
    "build": {
    "dockerfile": "Dockerfile",
    "context": ".."
  },
  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },
  "forwardPorts": [],
  "remoteUser": "vscode"
}
